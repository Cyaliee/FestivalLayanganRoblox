--!strict
-- Meja Rakit. Biaya upgrade & efek level ditambahkan di Fase 4.

return table.freeze({
	MaxLevel = 5,
	-- Attribute di model MejaRakit (dibaca client, mis. untuk billboard)
	LevelAttribute = "Level",
	OwnerAttribute = "OwnerUserId",
})
